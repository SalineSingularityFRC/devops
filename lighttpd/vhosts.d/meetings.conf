$HTTP["host"] == "meeting.5066.team" {
  var.server_name = "meeting.5066.team"

  server.name = server_name
  server.document-root = "/usr/local/www/meeting"
  accesslog.filename = log_root + "/" + server_name + "/access.log"
}
